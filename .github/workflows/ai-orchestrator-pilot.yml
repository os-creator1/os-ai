name: AI Orchestrator Pilot (disabled)

# Archived on purpose. This workflow formerly made metered OpenAI and Claude
# API calls. The subscription-only replacement is ai-subscription-loop.yml.
on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "The metered AI orchestrator pilot is disabled."

