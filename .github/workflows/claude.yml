name: Claude Code API (disabled)

# Archived on purpose. Claude implementation now runs through a subscription
# Routine and the label protocol documented in docs/automation/.
on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "The API-backed Claude workflow is disabled."

